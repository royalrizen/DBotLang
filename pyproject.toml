[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "dbotlang"
version = "0.1.0"
description = "A silly easy-to-use programming language for Discord bots."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]